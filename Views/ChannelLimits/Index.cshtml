@model IEnumerable<CRM_CUS.Models.ChannelLimit>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TrnDailyAmount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TrnMonthlyAmount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TrnDailyCount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TrnMonthlyCount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescriptionRu)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TransactionType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TrnDailyAmount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TrnMonthlyAmount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TrnDailyCount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TrnMonthlyCount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescriptionRu)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TransactionType.Description)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
